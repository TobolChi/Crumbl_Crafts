name: Backend Leaderboard Updates

on:
  schedule:
    - cron: '*/15 * * * *'   # every 15 minutes; adjust as needed
  push:
    paths:
      - 'backend/**'        # run when backend code changes
      - 'metrics_api.py'
      - 'requirements.txt'
  workflow_dispatch:

permissions:
  contents: write           # needed to push data branch
  id-token: write

jobs:
  update-leaderboard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        if: exists('requirements.txt')
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run leaderboard update script
        id: run_update
        run: |
          # Run your script; ensure it writes output to ./data/site_metrics.json (or similar)
          python metrics_api.py

      - name: Prepare data branch
        id: prepare_branch
        run: |
          DATA_BRANCH="data-updates"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Create or switch to data branch
          if git ls-remote --heads origin $DATA_BRANCH | grep -q $DATA_BRANCH; then
            git fetch origin $DATA_BRANCH:$DATA_BRANCH
            git checkout $DATA_BRANCH
          else
            git checkout --orphan $DATA_BRANCH
            git rm -rf .
            git commit --allow-empty -m "Initialize data branch"
            git push origin $DATA_BRANCH
          fi

      - name: Copy generated data
        run: |
          # Adjust paths to match your script output
          mkdir -p data
          cp -f site_metrics.json data/site_metrics.json || true
          cp -f site_metrics.json .last_backend_data || true
          git add data/site_metrics.json .last_backend_data || true
          if git diff --staged --quiet; then
            echo "No data changes to commit"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            git commit -m "chore: update leaderboard data [skip ci]" || true
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Push data branch
        if: steps.prepare_branch.outcome == 'success' && steps.run_update.outcome == 'success' && steps.prepare_branch.outputs.changed != 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push origin HEAD:data-updates --force

      - name: Finish
        run: echo "Backend update job completed"
